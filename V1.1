getgenv().PredictMovement = true -- Predicts if they are moving in fast velocity (like jumping) so the aimbot will go a bit faster to match their speed 
getgenv().PredictionVelocity = 21  -- use something high if ur ping is low, use something a little lower if if its higher ping
getgenv().CheckIfJumped = true
getgenv().Smoothness = true
getgenv().SmoothnessAmount = 0.38 -- legitness smoothness everything below 0.1 is legit smooth, 0.2, 0.23 - 0.3 is semi legit. and anything above is blatant


loadstring(game:HttpGet("https://raw.githubusercontent.com/qovk/CrimAssist/refs/heads/main/Source"))()
